import { Filter, FacetDistribution, FacetsCache, MeiliSearchParams, SearchContext } from '../../types';
/**
 * @param  {Filter} filters?
 * @returns {FacetsCache}
 */
export declare function getFacetsFromFilter(filters?: Filter): FacetsCache;
/**
 * @param  {Filter} filters?
 * @returns {FacetsCache}
 */
export declare function extractFacets(searchContext: SearchContext, searchParams: MeiliSearchParams): FacetsCache;
/**
 * Assign missing filters to facetDistribution.
 * All facets passed as filter should appear in the facetDistribution.
 * If not present, the facet is added with 0 as value.
 *
 *
 * @param  {FacetsCache} cache?
 * @param  {FacetDistribution} distribution?
 * @returns {FacetDistribution}
 */
export declare function addMissingFacets(cachedFacets?: FacetsCache, distribution?: FacetDistribution): FacetDistribution;
//# sourceMappingURL=filters.d.ts.map